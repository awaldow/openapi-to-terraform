using System;

namespace openapi_to_terraform.RevisionCli
{
    class Program
    {
        static void Main(string[] args)
        {
            var inputAssemblyPath = args[0];
            var openApiPath = args[1];
            var outputPath = args[2];
        }
    }
}
